<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Multicounter_v3">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboard|keyboardHidden|fontScale|locale|uiMode"
            >
            <!--
                - Tamaño de visualización de la app → screenSize
                - Orientación de la pantalla → orientation
                - Tamaño y grosor de la fuente (escala de fuente / font scale) → fontScale
                - Configuración regional (idioma, formato de números, etc.) → locale (en API nuevas se recomienda layoutDirection también)
                - Modo oscuro / claro → uiMode
                - Disponibilidad del teclado → keyboard y keyboardHidden
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
    </application>

</manifest>